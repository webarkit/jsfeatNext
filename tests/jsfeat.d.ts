// The original jsfeat (vendored under tests/vendor/) is used only as a
// golden-output *oracle* in the characterization tests. It ships no type
// declarations, so we declare it loosely.
declare module "*oracle.cjs";
