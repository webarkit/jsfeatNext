export declare class math {
    get_gaussian_kernel(kernel_size: number, sigma: number, filter: Int32Array | Float32Array, data_type: number): void;
    median(array: any, low: number, high: number): number;
}
